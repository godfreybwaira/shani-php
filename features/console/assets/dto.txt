<?php

namespace {namespace} {

    use {entity_ns}\{entity_name};

    final class {dto_name} implements \JsonSerializable
    {

        public readonly string $id;

        public function __construct(string $id)
        {
            $this->id = $id;
        }


        /**
         * Convert a given entity from {entity_name} to {dto_name} type
         * @param {entity_name} $entity {entity_name} object
         * @return {dto_name}
         */
        public static function entity2dto({entity_name} $entity): {dto_name}
        {
            $dto = new {dto_name}($entity->id);
            return $dto;
        }

        /**
         * Convert a given Dto from {dto_name} to {entity_name} type
         * @param {dto_name} $dto {dto_name} object
         * @return {entity_name}
         */
        public static function dto2entity({dto_name} $dto): {entity_name}
        {
            $entity = new {entity_name}($dto->id);
            return $entity;
        }

        #[\Override]
        public function jsonSerialize(): array
        {
            return [
                'id' => $this->id
            ];
        }
    }

}

