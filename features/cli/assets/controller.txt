<?php

namespace {namespace} {

    use {service_ns}\{controller_name}Service;
    use shani\http\HttpResponse;
    use gui\WebUIBuilder;
    use shani\launcher\App;

    final class {controller_name}
    {

        private readonly App $app;
        private readonly {controller_name}Service $service;

        public function __construct(App $app)
        {
            $this->app = $app;
            $this->service = new {controller_name}Service();
        }

        public function {fn_name}(): HttpResponse
        {
            return HttpResponse::withBody(new WebUIBuilder());
        }
    }

}

